<%= turbo_frame_tag "new_post" do %>
  <%= simple_form_for @post, html: { class: 'w-50 mt-3' } do |f| %>
    <%= f.input :title %>
    <%= f.input :content %>
    <%= f.submit class: 'btn btn-primary' %>
  <% end %>
<% end %>
